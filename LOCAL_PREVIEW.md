Local preview (quick)

1. Create the virtual environment and install deps:

```bash
bash scripts/create_venv.sh
```

2. Start the app:

```bash
bash scripts/run_app.sh
```

If you need the server to be accessible on a different port, set `PORT`, for example:

```bash
PORT=8503 bash scripts/run_app.sh
```
